#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/tests.v1.schema.json

test_format = 1.0

[default]

    # No previous packaged version to upgrade from yet.
    test_upgrade_from = []

    # Values used for the install questions during CI.
    [default.args]
    node_ip = "127.0.0.1"
    node_port = "4403"
